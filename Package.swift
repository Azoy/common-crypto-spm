// swift-tools-version:4.0

import PackageDescription

let package = Package(
    name: "SSCommonCrypto",
    products: [
        .product(
            .library(name: "SSCommonCrypto", targets: [])
        )
    ]
)
